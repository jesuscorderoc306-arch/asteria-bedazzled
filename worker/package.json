{
  "name": "asteria-worker-v2",
  "private": true,
  "type": "module",
  "scripts": {
    "build:panel": "node scripts/build-panel.mjs",
    "pretest": "node scripts/build-panel.mjs",
    "test": "node --test test/worker.test.mjs",
    "dev": "node scripts/build-panel.mjs && wrangler dev",
    "deploy": "node scripts/build-panel.mjs && wrangler deploy"
  }
}
